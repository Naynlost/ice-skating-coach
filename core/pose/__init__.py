"""Kisi tespiti (YOLO11), takip (ByteTrack), 2D poz (RTMPose).

Gorev 1.4 ve 1.5'te doldurulacak.

Sozlesme (taslak):
    track_athlete(frames) -> list[Track]      # hedef sporcuyu klip boyunca tut
    estimate_poses(frames, track) -> PoseSequence
"""
